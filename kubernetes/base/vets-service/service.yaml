apiVersion: v1
kind: Service

metadata:
  name: vets-service
  namespace: petclinic

  labels:
    app: vets-service

spec:
  selector:
    app: vets-service

  ports:
    - name: http
      port: 8083
      targetPort: 8083

  type: ClusterIP
