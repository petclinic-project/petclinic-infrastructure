apiVersion: v1
kind: Service

metadata:
  name: customers-service
  namespace: petclinic

spec:
  selector:
    app: customers-service

  ports:
    - port: 8081
      targetPort: 8081

  type: ClusterIP
